{
  "name": "musteri-takip-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute musteri-takip-crm --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute musteri-takip-crm --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}